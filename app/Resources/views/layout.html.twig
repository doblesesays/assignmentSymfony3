<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Asgnación de tareas{% endblock %}</title>
        {% block stylesheets %}
            {# <link rel="stylesheet" href="{{ asset('public/css/bootstrap.min.css') }}" /> #}
            <link rel="stylesheet" href="{{ asset('public/css/flatly.min.css') }}" />
            {# <link rel="stylesheet" href="{{ asset('public/css/journal.min.css') }}" /> #}
            {# <link rel="stylesheet" href="{{ asset('public/css/sandstone.min.css') }}" /> #}
            <link rel="stylesheet" href="{{ asset('public/css/style.css') }}" />
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                {{ include('menu.html.twig') }}
            {% elseif is_granted('IS_AUTHENTICATED_ANONYMOUSLY') %}
                {{ include('locale.html.twig') }}
            {% endif %}
        {% endblock %}
        {% block javascripts %}
            <script src="{{ asset('public/js/jquery-2.2.4.js') }}"></script>
            <script src="{{ asset('public/js/bootstrap.min.js') }}"></script>
            <script src="{{ asset('public/js/bootbox.min.js') }}"></script>
        {% endblock %}
    </body>
</html>
