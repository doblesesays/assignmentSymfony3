<nav class="navbar navbar-inverse" style="top:0;">
    <div class="container-fluid container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <a class="navbar-brand" href="">{% trans %}Assignments{% endtrans %}</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">{{'Language'|trans}} <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        <li>
                            <a href="{{ path(app.request.get("_route"), app.request.get("_route_params")|merge({'_locale':'en'})) }}">{{'English'|trans}}</a>
                        </li>
                        {# <li class="divider"></li> #}
                        <li>
                            <a href="{{ path(app.request.get("_route"), app.request.get("_route_params")|merge({'_locale':'es'})) }}">{{'Spanish'|trans}}</a>
                        </li>
                  </ul>
                </li>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>