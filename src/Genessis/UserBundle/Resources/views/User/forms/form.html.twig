{{ form_start(form) }}
	{{ form_widget(form) }}
	<input type="submit" value="{{ 'Delete user'|trans }}" class="btn btn-lg btn-block btn-danger" onclick="return confirm('Estás seguro?')">
{{ form_end(form) }}